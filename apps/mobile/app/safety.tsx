import { router } from 'expo-router'; import { StyleSheet, Text } from 'react-native'; import { Button } from '../src/components/Controls'; import { Card, Screen, Subtitle, Title } from '../src/components/Screen'; import { useSettingsStore } from '../src/stores/app'; import { colors } from '../src/theme';
export default function Safety() { const accept = useSettingsStore((s) => s.setSafetyAccepted); return <Screen><Title>Safety first</Title><Subtitle>Live vehicle data can distract you. Set up adapters and run diagnostics only while parked.</Subtitle><Card><Text style={styles.item}>• Keep your attention on the road.</Text><Text style={styles.item}>• Standard OBD reads are enabled. Unverified Toyota writes are not.</Text><Text style={styles.item}>• Diagnostic actions can change vehicle behavior and require explicit safeguards.</Text><Text style={styles.item}>• The app is not a substitute for professional diagnosis.</Text></Card><Button onPress={() => { accept(true); router.replace('/setup'); }}>I understand</Button></Screen>; }
const styles = StyleSheet.create({ item: { color: colors.text, fontSize: 17, lineHeight: 25 } });
