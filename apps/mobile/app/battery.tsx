import { StyleSheet, Text } from 'react-native'; import { Card, Screen, Subtitle, Title } from '../src/components/Screen'; import { priusCUs2013Profile } from '@prius/vehicle-profiles'; import { colors } from '../src/theme';
export default function Battery() { const entries = priusCUs2013Profile.telemetryDefinitions.filter((x) => !x.executable); return <Screen><Title>HV battery</Title><Subtitle>This screen is structurally ready, but it cannot poll Toyota data until commands are captured and independently verified.</Subtitle>{entries.map((entry) => <Card key={entry.id}><Text style={styles.label}>{entry.label}</Text><Text style={styles.status}>⊘ Unverified · disabled in production</Text><Text style={styles.detail}>{entry.verificationNeeded}</Text></Card>)}</Screen>; }
const styles = StyleSheet.create({ label: { color: colors.text, fontSize: 19, fontWeight: '800' }, status: { color: colors.attention, fontWeight: '700' }, detail: { color: colors.muted, lineHeight: 20 } });
